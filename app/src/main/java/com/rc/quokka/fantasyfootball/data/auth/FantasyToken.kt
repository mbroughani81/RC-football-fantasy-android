package com.rc.quokka.fantasyfootball.data.auth

import com.rc.quokka.fantasyfootball.domain.model.NoToken
import com.rc.quokka.fantasyfootball.domain.model.Token

object FantasyToken {
    var token: Token = NoToken
}